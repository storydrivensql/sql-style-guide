******************
Introduction: Why?
******************

SQL has surprisingly few style guides or discussion on style, given how verbose the syntax can be, and how lenient is by convention, especially with whitespace. There are a few good guides, as mentioned in :doc:`/content/resources`, from which this guide takes inspiration, and makes notable differences.
